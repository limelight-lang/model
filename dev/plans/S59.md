# S59 — The collector thread's birth takes its memory from the manager: notes

The role lines and the reasoning of `PLAN.md` S59, kept here because the
section would otherwise run past forty lines (`dev/INDEX.md`, Conventions).
Deleted with the stage; a ruling or a measurement goes to the journals first.

## The Critic's pass over the draft, 2026-09-18

Nine findings, each with what it changed:

1. **A guard page "through `memory::os`" needs an entry the module has
   not.** `map_aligned` maps read-write and the module declares `mmap` and
   `munmap` alone; glibc adds no guard to a user-provided stack. Without one,
   a frame past the low end of the mapping writes into whatever `mmap` placed
   below it — the next pool region, the next sibling's stack — and nothing
   faults. Accepted: `mprotect` is declared beside the two, the lowest page of
   the one mapping is turned `PROT_NONE`, and the guard is what makes the
   stack figure honest.
2. **The wake word's set and clear were unspecified, and both readings
   failed a case.** Set unconditionally with no clear, every pressure
   collection under forbidden births leaves the elder a stale wake and the
   next born elder runs an unasked round inside
   `a_poll_signals_once_a_registration_filled_a_block_and_not_before`'s
   quiet window; set only while alive, `end_idle_siblings` wakes an `ENDING`
   sibling into nothing and `the_siblings` waits out `PAST_THE_CASE`, and the
   tests' `retire`, which sets `RETIRING` outside any lock, loses its wake to
   a thread between its check and its wait. Accepted: `wake` sets the word
   under the mutex unconditionally and answers `state == ALIVE`;
   `begin_the_thread` clears it before storing `ALIVE`, where the handle was
   published; the stand-in clears it; `retire` sets every slot's word under
   its mutex and then notifies; the consent wait takes the word as the round
   wait does, else it returns at once until its deadline.
3. **`UNBORN` was stored before the runtime exit on the panic path.** The
   drop guard runs before `EXIT_GUARD`'s `ll_thread_exit` on an unwind, so a
   join at the next birth waits on an exit collection, and — speculative,
   unproven — a birth reached from that exit's own frames would join itself.
   Accepted: the trampoline runs `thread_body` under `catch_unwind`, then
   `ll_thread_exit`, then stores `UNBORN`; a join that fails is a refused
   birth, never a reuse of the stack.
4. **`pthread_attr_t` is opaque and platform-sized.** A struct declared too
   small is a silent overwrite of the caller's frame on the pressure path.
   Accepted: a 128-byte, 16-aligned opaque buffer with the real sizes stated
   beside it (56 on glibc and musl, 64 on macOS).
5. **Where an ended sibling's stack goes was unpriced.** Joined only at a
   rebirth that may never come, up to seven mappings and their zombie TCBs
   for the process's life. Accepted the cheaper shape: one stack per slot,
   mapped once and kept across lives, `MAX_COLLECTORS` the bound, so a birth
   after the first makes one join and one create and no `mmap`; a
   `pthread_create` that refuses keeps the stack, notes the refused birth and
   stores `UNBORN`.
6. **The red case had two readings.** Written as "red under a wait that
   sleeps without reading the word first".
7. **The Miri clause could be met by a run that saw nothing.** Every
   `unsafe` line the raw thread adds sits under `cfg(not(miri))`. Accepted:
   the stage's `done:` says the raw thread's own lines have no Miri coverage
   by construction, and the closing record names what covers them.
8. **Smaller omissions.** The name through `pthread_setname_np` on linux;
   `CloseHandle` and an `extern "system"` trampoline on Windows; the probe
   reads the crate's `#[global_allocator]` alone, so libc's own `calloc` of
   the DTV inside `pthread_create` is reported by the create's error code and
   is not the reading; a raw thread's panic prints to the real stderr under
   the harness — cosmetic, not changed.
9. **The stack size was unnamed.** `COLLECTOR_STACK_BYTES`, 2 MiB, std's
   default and not a measured figure: the suite passes under it in the debug
   profile, whose frames are the larger; the trace's worklist is arena-backed
   so no frame scales with the graph. A probe exists — lower the constant
   under the worker suite until it dies — and the record says so rather
   than claiming none.

What the Critic checked and found sound: the join-before-create order (the
state goes `UNBORN` only from the slot's own thread, and a join returns after
the kernel clears the tid, after every TLS destructor); the wake protocol
its callers rely on — false means the poll's flag stands, the answer is
ignored elsewhere — with an unconditional set and the answer read from the
state, and `ENDING` answering false where the handle answered true, an
improvement; `Mutex`/`Condvar` under Miri block the interpreted thread as
`park_timeout` does; no case reads a collector panic through a join; TLS
destructors run on a `pthread_create` thread through `__call_tls_dtors`; the
S59.1 → S59.2 order, separable, with `retire` and `HANDLES` ending with two
arms each.

## The Critic's pass over the finished stage, 2026-09-18

Four findings; the first three accepted, the fourth recorded:

1. **Join-before-create was a property of the clock.** The parent stored
   the thread into the slot after `pthread_create` returned, while the child
   stores `UNBORN` itself, so a child that ended inside the parent's publish
   gap — ended by a lowered cap, or by a panic in its first round — left a
   slot that read unborn with no thread to join, and the next birth would
   have created on the stack the first thread was still leaving. Fixed: a
   birth holds the slot's lock from the join through the create to the
   store on every arm, so a join made meanwhile waits for the store; and
   the tests' `join_every_slot` passes until one joins nothing, since a
   thread joined in one pass may have birthed a slot the pass had visited.
2. **On the pthread-backed unixes std's locks box themselves on first
   use.** macOS and the BSDs other than freebsd, openbsd and dragonfly: the
   slot's wake word, the slot lock and `REFUSED_AT` (which predates the
   stage) would each allocate once on the first pressure collection there.
   Recorded rather than fixed, in the residual line "The collector's birth
   has no run off Linux": the crate's targets are linux and windows, both
   futex-backed, and a build for the others owes the locks a first touch
   outside the pressure path or a futex the crate declares itself.
3. **"Unborn only when the thread has nothing of the runtime left to run"
   was stronger than the code.** After the word, glibc's TLS destructors
   run the exit guard's own `ll_thread_exit` pass — over a thread with
   nothing left, taking the pool's and the registry's locks and none a birth
   holds — and the join waits on that too. The sentence is corrected in the
   module doc and the `done:`, and the order the code does keep is read by
   `the_word_goes_unborn_after_the_threads_own_runtime_exit`: the thread
   records its exit-sequence count just before the word, and the case reads
   at least one — zero under the swapped order.
4. **Two refusal branches had no case.** `pthread_create`'s refusal now has
   `REFUSE_NEXT_CREATE` and `a_refused_create_is_a_birth_a_later_call_repeats`;
   the guard's `mprotect` failure and a failed join have no arm — glibc gives
   no such failure for a joinable thread — and the module doc says so.

Three asides, low: the guard case asserts the stack's mapping is exactly
2 MiB, which a later anonymous mapping of at most 64 KiB landing at the
stack's end and merging with it would break — no source of one found in the
test binary; a raw thread has no handler stack, so an overflow onto the
guard is a bare `SIGSEGV` rather than a named death, and the constant's doc
now says so; and a wake landing in the last instant of the consent wait is
neither spent nor remembered, a pre-existing bound of `FALLBACK_INTERVAL_MAX`.

What the Critic checked and found sound: one CAS from `UNBORN` succeeds per
unborn period; `end_idle_siblings` CASes from `ALIVE` alone; a `STARTING`
slot at `retire` reads `retiring()` at its loop head; the pressure path's
join holds no lock the ending thread takes; the windows declarations' ABI
and constants; the `pthread_attr_t` buffer, `pthread_t`, the name's length;
`map_aligned`'s contract for the stack's mapping and the one `unmap`; and
the S59.1 cases read what they claim.

## The Code Reviewer's pass, 2026-09-18 — received at the session's end

Applied: **1.1**, the Miri test crate did not compile — four cases gated on
linux/unix named `birth`'s `cfg(not(miri))` items under a `cfg_attr(miri,
ignore)`, and an ignore keeps a body; they carry `cfg(not(miri))` now, the
source-reading guard reads that as the exclusion, and the two wake cases
ran green under Miri (`d8b905e`).

Not yet applied — the next session's list, before the stage is deleted:

- **1.2** The slot protocol (lock, join what stood, create, store; the
  join) is written three times across the arms and the miri arm has drifted
  (its `join_under` answers nothing, its table is `HANDLES`, the names stand
  twice). Shape proposed: one `SLOTS: [Mutex<Option<platform::Thread>>; N]`,
  one `spawn`/`join`/`join_under` in the outer module over
  `platform::create(index) -> Option<Thread>` and `platform::join(Thread) ->
  Result<(), Thread>`; each arm exports `type Thread`, `create`, `join`,
  `name_this_thread`; one NUL-terminated name table.
- **1.3** Contracts: `has_no_thread`'s doc (a slot reading unborn may still
  hold an OS thread in its teardown); `ensure_thread`'s doc does not say the
  pressure path now joins the slot's last thread (its callers in
  `collect.rs` and `queue.rs` read it as non-blocking); the module doc's
  "glibc gives no such failure" overstates — `EDEADLK` on a self-join is
  the one documented shape; `join`'s doc omits the failed-join value; `wake`'s
  doc omits the wake sent between `forget_wakes` and the `ALIVE` store
  (cleared, answered true, stood in for by the birth's first round).
- **1.4** `const _: () = assert!(COLLECTOR_STACK_BYTES % STACK_GUARD_BYTES ==
  0)`, since `map_aligned` asks a multiple of the alignment.
- **1.5** Names: "word" names both the state and the wake — say "wake word"
  for `woken` everywhere; `woken`/`wakes` → `wake_pending`/`wake_signal`;
  `stand_down_as_the_elder` is empty — delete it and its three calls.
- **2.1–2.5** The "cleared at the birth" fact told six times, keep the
  field's; stale "wake token" at `Standing`'s doc and `the_batch.rs`; the
  `retire` clause about a panic inside the drop; "the poll having a frame
  that may allocate" at `worker.rs` and `queue.rs` justifies nothing now;
  retellings in `birth.rs` (the `catch_unwind` line, the `add` line, the
  lock comment, "Both entries" → "Every entry", "a short process" → "a
  process out of threads"); three refused-birth cases share a twenty-line
  tail and two panic cases share a body — helpers; `dev/ARCHITECTURE.md`
  names `memory/os` twice in the `cycle` row; `join_every_slot`'s fold
  depends on operand order — use `|`; `rfc/dev/design/trace-token-handshake.md`
  still argues over `park`'s token and "the slot's handle mutex with an
  `unpark`".
