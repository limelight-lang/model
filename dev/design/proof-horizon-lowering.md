# GC horizon reading aids — absorbed into the RFC

> **Moved 2026-08-20.** The catalogue of compiler-side structures and
> the per-function lowering walkthrough are now sections of
> `model/gc/gc-horizon-states.md` in the `rfc` repository, and the
> worked `total(Cart)` example is the filled template of
> `model/gc/gc-horizon-cases/README.md`. Nothing here was dropped; the
> three documents were merged so the normative surface is one text.
